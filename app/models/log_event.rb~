class event

  // event_type:source:event data...
  def parse(event_string)
    md = event_string.match("([^:]+):([^:]+):(.+)")
    event = case md[1]
            when "T" then TimePeriod.new()
            end
    
    event.source = md[2]
    event.parse(md[3])
    return event
  end

end
